# Setup Alpine OS on BeagleBone Black

This directory contains directories needed to setup Alpine OS project on BeagleBone Black.
Unfortunately we did not manage to make this OS work properly on BeagleBone Black, so this 
directory contains work that was done, without granting that it will work properly.

